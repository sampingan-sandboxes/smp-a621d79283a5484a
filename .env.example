# Cognito (JWT verification in the real auth module — the sandbox ships a decode-only stub).
COGNITO_USER_POOL_ID=
COGNITO_APP_CLIENT_ID=
# MongoDB connection (playbook run persistence).
MONGODB_URI=
MONGODB_NAME=
# Composio API key (trigger type listing + tool execution).
COMPOSIO_API_KEY=
# Anthropic API key (the gate + executor Claude calls).
ANTHROPIC_API_KEY=
# Async worker Lambda that the webhook hands playbook processing off to.
PLAYBOOK_PROCESSOR_FUNCTION_NAME=
